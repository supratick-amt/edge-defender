# P3 — Adaptive Connection Blocker *(NOT IMPLEMENTED)*
# =====================================================
# Placeholder. See README.md for the implementation plan.

services:
  not_implemented:
    image: alpine:3.19
    command: >
      sh -c "echo 'P3 gatekeeper not implemented yet. See targets/p3-fail2ban/README.md' && exit 1"
